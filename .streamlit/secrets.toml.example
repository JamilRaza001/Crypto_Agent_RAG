# Streamlit Secrets Configuration
# Copy this to Streamlit Cloud's secrets management
# DO NOT commit secrets.toml with real API keys!

# Google Gemini API
GEMINI_API_KEY = "your-gemini-api-key-here"

# ChromaDB Settings (use relative paths for Streamlit Cloud)
CHROMA_DB_PATH = "./data/chromadb"
SQLITE_DB_PATH = "./data/sqlite/metadata.db"

# Model Settings
EMBEDDING_MODEL = "sentence-transformers/all-MiniLM-L6-v2"
GEMINI_MODEL = "gemini-2.5-flash"
TEMPERATURE = "0.1"
MAX_TOKENS = "2048"

# RAG Settings
SIMILARITY_THRESHOLD = "0.5"
TOP_K_RESULTS = "5"
CHUNK_SIZE = "1000"
CHUNK_OVERLAP = "200"

# Cache Settings
PRICE_CACHE_TTL = "60"
HISTORICAL_CACHE_TTL = "3600"
TECHNICAL_CACHE_TTL = "300"

# Rate Limiting
FREECRYPTO_MONTHLY_LIMIT = "100000"
GEMINI_RPM_LIMIT = "60"

# Logging
LOG_LEVEL = "INFO"
